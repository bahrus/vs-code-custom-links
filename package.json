{
  "name": "custom-link-attributes",
  "displayName": "Custom Link Attributes",
  "description": "Make custom HTML attributes behave like hyperlinks",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:html"
  ],
    "repository": {
    "type": "git",
    "url": "git+https://github.com/bahrus/vs-code-custom-links.git"
  },
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Custom Link Attributes",
      "properties": {
        "customLinkAttributes.attributes": {
          "type": "array",
          "default": [],
          "description": "List of custom HTML attributes to treat as links (in addition to src and href)",
          "items": {
            "type": "string"
          }
        }
      }
    }
  },
  "dependencies": {
    "node-html-parser": "7.0.1"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
     "update": "ncu -u && npm install"
  },
  "devDependencies": {
    "@types/vscode": "^1.60.0",
    "@types/node": "^24.x",
    "typescript": "^5.9.3"
  }
}